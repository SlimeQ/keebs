# Keebs

Windows-native predictive on-screen keyboard.

## Install

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -Command "irm https://raw.githubusercontent.com/SlimeQ/keebs/main/scripts/install.ps1 | iex"
```

That command downloads the latest `SlimeQ/keebs` GitHub release MSI and runs the installer.
